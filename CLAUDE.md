# CLAUDE.md

このファイルは SanDBox プロジェクトで作業する際、Claude Code が常に踏まえるべき
前提を示す。

## SanDBoxとは

DBエンジンとデータ領域を1つの実行ファイル内に保持する、環境構築不要の
ポータブルな単一バイナリRDBMS。Go製、内部エンジンは `modernc.org/sqlite`。
**ネットワーク待受を一切持たない**——他システムとの結合は、子プロセスとして
起動して標準入出力でやり取りする stdio プロトコル、および素のSQLiteファイルの
読み書きで行う。詳細は `docs/spec/san-db-ox_spec_ja.md` を参照。

前身プロジェクトとして **ExecDB**（PostgreSQL互換ワイヤープロトコルによる
外部I/Fを持つ）があり、技術的な知見の一部を引き継いでいるが、**設計判断は
別物である**。特に外部I/F・アクセス制御・認証まわりの構造は意図的に廃した。
ExecDBのコードや設計を参照する場合も、判断は必ず SanDBox の仕様書を優先すること。

## 参照すべきファイル

作業を始める前に、以下を確認すること。

- **`docs/spec/san-db-ox_spec_ja.md`** — SanDBoxの仕様。コアコンセプト、
  インターフェース、REPLコマンド体系、保存・永続化仕様、非対話実行モード、
  SQLiteファイル入出力、stdioプロトコル、ユースケース、ライブラリ
  アーキテクチャ、技術選定、起動オプション、ライフサイクルまでを網羅する
  （テスト方針・開発フェーズ・配布方法は `.claude/rules/` 側の役目であり、
  ここには含まれない）。
  **実装しながら育てていくドキュメントであり、実装と仕様がズレた場合は
  このファイルを更新すること。**（`docs/usage/`・`docs/examples/`はこの
  仕様書とは別の、利用者向けドキュメント。詳細は`.claude/rules/directory-structure.md`
  参照）
- **`PLAN.md`** — 実装計画・進捗管理（開発フェーズ、現在地、保留事項）。
  リポジトリ直下に置く一級ドキュメント（`docs/spec/`とは異なり、Claude Code
  自身の作業ログという性質上、リポジトリ直下に留めている）。
  **セッションをまたぐたびに「現在地」を確認・更新すること。**
- **`.claude/rules/naming.md`** — 命名規則（製品名の3段階表記、REPLコマンド・
  CLIオプション・stdio op・`engine` API名の対応関係、短縮フラグ、タイムスタンプ
  生成ルール）
- **`.claude/rules/cli-output.md`** — CLI出力（言語、モードごとの出力先の
  使い分け、出力モード、JSONにおける値の表現、ログ出力先、保存確認プロンプトの
  扱い、読み取り専用モードの出力）
- **`.claude/rules/testing.md`** — テスト方針（開発環境の前提、実装後の
  動作確認について、SanDBox固有の検証項目、過去に踏んだ落とし穴）
- **`.claude/rules/directory-structure.md`** — ディレクトリ構成（`engine`と
  `cmd/san-db-ox`の役割分担、配置の判断基準）
- **`.claude/rules/distribution.md`** — 配布方法（GitHub Releases、
  `go install`、リポジトリへのバイナリ非コミット方針）
- **`.claude/rules/binary-size.md`** — 実行ファイルサイズ（積極的な最適化は
  不要だが、むやみに肥大化する実装は避ける）
- **`.claude/rules/sqlite-quirks.md`** — SQLite/`modernc.org/sqlite`特有の
  落とし穴（Backup APIの挙動、`memdb` VFS固有の制約等、仕様書だけでは
  判断できない実装上の注意点）

これらのルールファイルは、実装中に得た細かい気づき・教訓を Claude Code 自身が
育てていくものである。新しく気づいたルール・過去に踏んだ落とし穴などがあれば、
該当するファイルに追記すること。どのファイルにも当てはまらない新しい種類の
気づきであれば、新しいルールファイルを作ってよい（例: SQLite/
`modernc.org/sqlite`特有の落とし穴が溜まってきたら `sqlite-quirks.md` を
新設する、など）。

なお `testing.md`・`naming.md` の一部には「**出所: 前身プロジェクト ExecDB**」と
注記された節がある。これらはExecDBの開発中に実際に踏んだ事象であり、SanDBoxで
発生したものではない。SanDBoxでも起こりうるため記録として引き継いでいる。
**SanDBoxで新たに踏んだ事象を追記する際は、この注記を付けないこと**（出所が
混ざると、どちらの記録か分からなくなる）。

## 開発の進め方

- 現在の開発フェーズは `PLAN.md` の「現在地」を確認すること。
  フェーズが進んだら、そちらを更新すること。
- 実装後は `.claude/rules/testing.md` に従い、ビルド確認（`make build` 等）に
  加えて実際の動作確認（`make test` 等）を行うこと。判断に迷ったら実行する
  側に倒すこと。
- 依存ライブラリを追加する場合、ライセンスがMIT（本プロジェクトのライセンス）
  と衝突しないか確認すること（GPL系等は避ける。詳細は `.claude/rules/testing.md`
  の trivy チェックの節を参照）。
- **設計判断を変える場合は、まず仕様書を更新してから着手すること。** 実装が
  先行して仕様書が後追いになると、どちらが正なのか分からなくなる。

## ドキュメントの書き方

**提供しない機能については、事実だけを書き、代替手段を書かない。** 例えば
「行を逐次取得するカーソル機構は持たない」とだけ記し、`LIMIT` やキーセット
ページングといった回避策は書かない。仕様書は「何を保証するか」を書く場所で
あり、使い方の指南を書くと、**書いていない解法が選ばれにくくなって実装者の
視野を狭める**。同じ理由で、ドライバに `iterate` 系のヘルパーを標準で持たせる
こともしない。

**例外:** 守らないと動かないもの（stdioプロトコルのフラッシュ要求、stderrを
塞がない、EOFは接続断、stdinクローズで終了）は、使い方の助言ではなく
**契約**なので仕様書に書く。両者を混同しないこと。

「あると期待されやすいのに無い機能」は、無いことを明記する価値がある。
「無い機能を際限なく列挙する」のとは違うので、そこは判断すること。

## ドキュメントの言語

- **`.claude/rules/`・`PLAN.md`・`CLAUDE.md` は日本語のみ。** 英語版は作らない。
- **`docs/` 配下は日本語版を先に作り、実装完了後に英語版を整える。**
  日本語版のファイル名には `_ja` サフィックスを付ける
  （`san-db-ox_spec_ja.md`、`docs/usage/README_ja.md` 等）。英語版は同名から
  `_ja` を取ったファイル名で後から追加する。
- **コード・コメント・CLI出力は英語。** 対話は日本語で行うが、これらは英語で
  書くこと。両者を混同しないこと（詳細は `.claude/rules/cli-output.md`）。

## 権限・自動化について

`.claude/settings.json` により、`git push`/`pull`/`fetch`/`clone`、
`curl`/`wget`、パッケージインストール系コマンド（`*install*`, `go get`）、
`sudo` は実行前に確認を求める設定になっている。確認を求められた場合、無理に
実行しようとせず、指示を仰ぐこと。

ビルドの自動フック（`go build`/`make build` 等）は現時点では設定していない
（`Makefile` の中身が固まっていないため）。**フェーズ①のStep 1で `Makefile` を
作った時点で、`PostToolUse` フックの設定を提案すること**（詳細は `PLAN.md` の
「保留事項」参照）。

## ルール・スキルの提案

作業を進める中で、以下に気づいたら、都度こちらから提案すること（提案するだけで、
勝手に作成・適用はしない。判断はこちらが行う）。

- **新しいルールにした方がよさそうな知見**: 同じ種類の判断や落とし穴に複数回
  遭遇した、既存の `.claude/rules/` のどれにも当てはまらない、といった場合。
  どのファイルに追記すべきか、あるいは新規ファイルが必要かも合わせて提案する。
- **Skill化した方が効率的そうな作業**: 「同じ手順を3回目繰り返している」など、
  パターン化できそうな一連の作業に気づいた場合。まだ実装が薄い段階では
  無理に提案する必要はない。パターンが実際に繰り返されてから提案すること。

提案は気づいたタイミングで随時行ってよく、まとめて報告するために貯めておく
必要はない。
